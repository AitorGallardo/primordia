{
  "name": "primordia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A tiny living world where small open-source language models are the minds of autonomous organisms, running entirely in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.3.3",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  }
}
